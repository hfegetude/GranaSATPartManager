language: node_js
node_js:
  - 8
  - 10
cache:
  - yarn: true
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version version-number
  - export PATH="$HOME/.yarn/bin:$PATH"
script:
  - cd granasat_pm_client
  - yarn
  - yarn test
  - yarn build


